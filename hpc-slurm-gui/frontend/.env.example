# ─── API Configuration ───────────────────────────────────
VITE_BACKEND_API_BASE_URL=/api           # Backend API base URL (proxied through Nginx)
VITE_MASTER_PORT=5053                    # Slurm Master API port
